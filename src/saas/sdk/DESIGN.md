# sdk — DESIGN

Purpose
- SDK helpers and lightweight client utilities used by SaaS submodules (wrappers around internal APIs and typed clients).

Public API
- Typed client constructors, small helpers for request shaping, and response mappers.

Testing
- Unit tests for mappers and small integration tests against function stubs.

TODO
- Add `sdk/facade.ts` and typed request/response interfaces.
