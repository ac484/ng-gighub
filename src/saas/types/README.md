# types — README

Overview
- Shared domain types for SaaS (tenants, subscriptions, pricing models).

Quickstart
- Import from `src/saas/types` and use the typed interfaces in repos and facades.

File layout
- `index.ts` — barrel export of stable interfaces
