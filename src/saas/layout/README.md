# layout — README

Overview
- Shared layout building blocks (header, nav, main container) and accessibility helpers.

Quickstart
- Import the available layout components and include the `Skip to main` link as the first focusable element.

File layout
- `layout.facade.ts` — small helpers for page-level states
- `components/` — reusable layout components
