# utils — README

Overview
- Collection of small helpers used by saas facades and components.

Quickstart
- Import utilities from `src/saas/utils` and prefer small focused functions over large helpers.

File layout
- `index.ts` — barrel export
- `date.ts`, `validation.ts` — examples of helper files
