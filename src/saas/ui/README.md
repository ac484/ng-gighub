# ui — README

Overview
- Accessible, theme-aware presentational components used across SaaS pages.

Quickstart
- Import the component directly (they are standalone) and pass typed inputs from signals.

File layout
- `components/` — each component in its own folder with story/test files
