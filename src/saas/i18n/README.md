# i18n — README

Overview
- Message bundles and helper facade for translations used across SaaS UI components.

Quickstart
- Use `I18nFacade.translate('saas.tenant.title')` in components or templates.

File layout
- `messages/en.json`, `messages/zh-TW.json` — message bundles
- `i18n.facade.ts` — public translate helper
