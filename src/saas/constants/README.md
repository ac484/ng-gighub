# constants — README

Overview
- Central place for stable string constants, collection names, and runtime flags used across saas modules.

Usage
- Import `from 'src/saas/constants'` and reference constants directly.

File layout
- `index.ts` — barrel export
- `collections.ts` — collection name constants
- `flags.ts` — runtime feature flags
