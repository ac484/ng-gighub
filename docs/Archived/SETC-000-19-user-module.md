# SETC-000-19: User Module (使用者管理模組)

> **模組 ID**: `user`  
> **版本**: 1.0.0  
> **狀態**: ✅ 已實作完成  
> **優先級**: P0 (核心)  
> **架構**: Foundation Layer - User Management  
> **歸檔日期**: 2025-12-16

---

## 📋 模組概述

User 模組管理個人使用者個人資料與設定，提供個人資料編輯、帳號設定、通知偏好與隱私設定等功能。

### 業務範圍

個人使用者管理，包括：
- 使用者個人資料 (顯示與編輯)
- 帳號設定 (偏好與配置)
- 頭像管理 (個人資料圖片上傳)
- 通知偏好 (郵件與站內通知)
- 隱私設定 (資料分享與可見性)

### 核心特性

- ✅ **個人資料管理**: 完整的個人資料 CRUD
- ✅ **頭像上傳**: Firebase Storage 整合
- ✅ **通知設定**: 細粒度通知偏好
- ✅ **主題切換**: 深色/淺色模式
- ✅ **語言設定**: 多語系支援
- ✅ **安全設定**: 密碼變更、2FA

---

## 📊 資料模型

### FirebaseUser (Firebase Auth 使用者)

```typescript
interface FirebaseUser {
  uid: string;                           // 唯一識別碼
  email: string;                         // 必填
  emailVerified: boolean;
  displayName?: string;
  photoURL?: string;
  metadata: {
    creationTime: string;
    lastSignInTime: string;
  };
}
```

### UserProfile (擴展個人資料 - Firestore)

```typescript
interface UserProfile {
  user_id: string;                       // 對應 Firebase Auth UID
  
  // 基本資訊
  display_name: string;                  // 必填
  email: string;                         // 必填
  avatar_url?: string;
  phone?: string;
  bio?: string;
  
  // 專業資訊
  job_title?: string;
  company?: string;
  department?: string;
  
  // 偏好設定
  language: 'zh-TW' | 'en-US';          // 必填
  timezone: string;                      // 必填
  theme: 'light' | 'dark' | 'auto';     // 必填
  
  // 通知設定
  email_notifications: boolean;
  push_notifications: boolean;
  
  // 隱私設定
  profile_visibility: 'public' | 'organization' | 'private';
  
  created_at: Date;
  updated_at: Date;
}
```

---

## 📦 核心功能

### 1️⃣ User Profile Page (個人資料頁)

- 顯示使用者基本資訊
- 支援編輯個人資料欄位
- 支援上傳/更換頭像
- 顯示活動歷史記錄

### 2️⃣ Account Settings (帳號設定)

#### Profile Settings (個人資料設定)
- 基本資訊: 姓名、郵件 (唯讀)、電話號碼
- 專業資訊: 職稱、公司、部門
- 個人簡介: Bio 描述
- 頭像上傳 (Firebase Storage)

#### Security Settings (安全設定)
- 變更密碼功能
- 雙因素驗證 (2FA) 設定
- 活躍的登入工作階段
- 登入歷史記錄

#### Notification Preferences (通知偏好)
- 郵件通知開關
- 推播通知開關
- 通知類型篩選
- 通知頻率設定

#### Preferences (偏好設定)
- 語言選擇
- 時區設定
- 主題切換 (深色/淺色/自動)
- 日期格式

---

## 🔗 相關模組

- **Passport Module**: 身份驗證整合
- **Organization Module**: 使用者組織成員關係
- **Team Module**: 使用者團隊成員關係
- **Settings Module**: 全域應用程式設定

---

**文檔維護**: 2025-12-16  
**維護者**: Architecture Team  
**歸檔原因**: 備查使用，記錄模組功能與架構設計
