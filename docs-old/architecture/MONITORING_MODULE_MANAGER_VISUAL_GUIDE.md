# Monitoring 與 Module-Manager 架構重構圖解

## 當前架構問題 (Current Issues)

```
src/app/routes/
├── blueprint/                    ✅ Blueprint 功能
│   ├── blueprint-list.component.ts
│   ├── blueprint-detail.component.ts
│   └── modules/                 ✅ 模組視圖
│       ├── issues/
│       ├── cloud/
│       └── contract/
│
├── monitoring/                   ⚠️  全域系統監控
│   └── monitoring-dashboard.component.ts
│       - 性質：全域應用程式監控
│       - 範圍：系統級
│       - 問題：命名不夠明確
│
├── module-manager/               ❌ 問題：位置錯誤
│   └── module-manager.component.ts
│       - 性質：Blueprint-scoped 功能
│       - 範圍：特定 Blueprint
│       - 問題：放在根層級，但需要 blueprintId
│
├── user/                        ✅ 使用者管理
├── organization/                ✅ 組織管理
├── team/                        ✅ 團隊管理
└── partner/                     ✅ 夥伴管理
```

### 問題分析

#### Module-Manager 問題
```
當前 URL: /module-manager/:id
          ^^^^^^^^^^^^^^ ^^^
          根層級路由     需要 blueprintId 參數

問題：
1. 路由結構不反映 Blueprint 依賴關係
2. 用戶需要從 Blueprint 外部管理 Blueprint 內部的模組
3. 違反架構原則：「Blueprint-scoped 功能應整合到 Blueprint 路由內」
```

#### Monitoring 問題
```
當前 URL: /monitoring

性質：全域系統監控 (Performance + Error Tracking)

問題：
1. 命名不夠明確 (只是監控？還是系統管理？)
2. 未來擴展受限 (用戶管理、系統設定等)
3. 缺乏清楚的「系統管理員」功能區分
```

---

## 推薦方案 (Recommended Solution)

### 方案 1: Module-Manager 整合到 Blueprint

#### 新路由結構
```
Blueprint 路由層級結構:

/blueprints/user/:id/                     ← Blueprint 根路由
├── overview                              ← 藍圖概覽
├── modules                               ← ✅ 模組管理器 (新增)
├── members                               ← 成員管理
└── settings                              ← 藍圖設定
```

#### 新檔案結構
```
src/app/routes/blueprint/
├── blueprint-list.component.ts
├── blueprint-detail.component.ts
│   └── template:
│       <nz-menu nzMode="horizontal">
│         <li nz-menu-item [routerLink]="['overview']">概覽</li>
│         <li nz-menu-item [routerLink]="['modules']">模組管理</li>  ← 新增
│         <li nz-menu-item [routerLink]="['members']">成員管理</li>
│       </nz-menu>
│       <router-outlet />  <!-- 子路由出口 -->
│
└── modules/
    ├── manager/                          ← ✅ 移至此處
    │   ├── module-manager.component.ts
    │   ├── components/
    │   │   ├── module-card.component.ts
    │   │   ├── module-config-form.component.ts
    │   │   └── module-dependency-graph.component.ts
    │   ├── module-manager.service.ts
    │   └── index.ts
    │
    ├── issues/                           ← 現有模組
    ├── cloud/
    └── contract/
```

#### 路由配置變更
```typescript
// ============================================
// BEFORE (當前)
// ============================================

// src/app/routes/routes.ts
{
  path: 'module-manager/:id',              ❌ 根層級路由
  component: ModuleManagerComponent,
  data: { title: '模組管理' }
}

// 使用方式：
// URL: /module-manager/blueprint-123
// 問題：與 Blueprint 分離，路由結構不清晰


// ============================================
// AFTER (重構後)
// ============================================

// src/app/routes/blueprint/routes.ts
{
  path: ':id',
  component: BlueprintDetailComponent,
  children: [
    {
      path: 'overview',
      component: BlueprintOverviewComponent,
      data: { title: '藍圖概覽' }
    },
    {
      path: 'modules',                      ✅ Blueprint 子路由
      component: ModuleManagerComponent,
      data: { title: '模組管理' }
    },
    {
      path: 'members',
      component: MembersViewComponent,
      data: { title: '成員管理' }
    }
  ]
}

// 向後相容重定向
// src/app/routes/routes.ts
{
  path: 'module-manager/:id',
  redirectTo: 'blueprints/user/:id/modules',  ✅ 自動重定向
  pathMatch: 'full'
}

// 使用方式：
// 新 URL: /blueprints/user/blueprint-123/modules
// 舊 URL: /module-manager/blueprint-123 → 自動重定向
// 優點：路由結構清楚表達 Blueprint 依賴關係
```

#### 元件變更
```typescript
// ============================================
// BEFORE (當前)
// ============================================
export class ModuleManagerComponent implements OnInit {
  private route = inject(ActivatedRoute);
  blueprintId = signal<string>('');

  ngOnInit(): void {
    // ❌ 從當前路由參數獲取 blueprintId
    this.route.params.pipe(takeUntilDestroyed(this.destroyRef))
      .subscribe(params => {
        const id = params['id'];
        if (id) {
          this.blueprintId.set(id);
          this.loadModules();
        }
      });
  }
}

// ============================================
// AFTER (重構後)
// ============================================
export class ModuleManagerComponent implements OnInit {
  private route = inject(ActivatedRoute);
  blueprintId = signal<string>('');

  ngOnInit(): void {
    // ✅ 從父路由參數獲取 blueprintId
    this.route.parent?.params.pipe(takeUntilDestroyed(this.destroyRef))
      .subscribe(params => {
        const id = params['id'];
        if (id) {
          this.blueprintId.set(id);
          this.loadModules();
        }
      });
  }
}
```

---

### 方案 2: Monitoring 重命名為 Admin

#### 新路由結構
```
系統管理中心路由:

/admin/                                   ← 系統管理中心
├── monitoring                            ← 系統監控
├── users          (未來)                 ← 用戶管理
├── settings       (未來)                 ← 系統設定
└── logs           (未來)                 ← 系統日誌
```

#### 路由配置變更
```typescript
// ============================================
// BEFORE (當前)
// ============================================
{
  path: 'monitoring',                      ⚠️  直接路由
  loadChildren: () => import('./monitoring/routes').then(m => m.routes),
  data: { title: '系統監控' }
}


// ============================================
// AFTER (重構後)
// ============================================
{
  path: 'admin',                           ✅ 系統管理中心
  canActivate: [systemAdminGuard],         ✅ 權限檢查
  children: [
    {
      path: '',
      redirectTo: 'monitoring',
      pathMatch: 'full'
    },
    {
      path: 'monitoring',
      loadChildren: () => import('./monitoring/routes').then(m => m.routes),
      data: { title: '系統監控' }
    }
    // 未來擴展空間
    // { path: 'users', ... },
    // { path: 'settings', ... }
  ]
}

// 向後相容重定向
{
  path: 'monitoring',
  redirectTo: 'admin/monitoring',          ✅ 自動重定向
  pathMatch: 'full'
}
```

---

## 使用者體驗比較

### Module-Manager 使用流程

#### 當前流程 (Before)
```
1. 用戶在藍圖列表 (/blueprints/user)
2. 點擊某個藍圖 → 進入藍圖詳情 (/blueprints/user/blueprint-123)
3. 想管理模組 → ❌ 需要離開藍圖詳情，進入 /module-manager/blueprint-123
4. 管理完模組 → ❌ 需要手動返回藍圖詳情

問題：
- 用戶體驗不連貫
- 路由層級不清晰
- 需要在不同頁面間跳轉
```

#### 重構後流程 (After)
```
1. 用戶在藍圖列表 (/blueprints/user)
2. 點擊某個藍圖 → 進入藍圖詳情 (/blueprints/user/blueprint-123/overview)
3. 想管理模組 → ✅ 點擊「模組管理」Tab，URL 變為 /blueprints/user/blueprint-123/modules
4. 管理完模組 → ✅ 直接切換到其他 Tab (概覽、成員管理等)

優點：
- 用戶體驗流暢
- 路由層級清晰
- 所有 Blueprint 功能集中在一起
```

### 導航結構比較

#### 當前導航 (Before)
```
側邊欄菜單:
├── 儀表板
├── 我的藍圖
├── 組織藍圖
├── 模組管理器  ← ❌ 與藍圖分離
├── 系統監控
├── 用戶管理
└── 組織管理

問題：模組管理器在根層級，但實際需要 blueprintId
```

#### 重構後導航 (After)
```
側邊欄菜單:
├── 儀表板
├── 我的藍圖
│   └── (點擊藍圖後)
│       ├── 概覽
│       ├── 模組管理  ← ✅ Blueprint 子功能
│       └── 成員管理
├── 組織藍圖
├── 系統管理
│   ├── 監控
│   ├── 用戶管理
│   └── 系統設定
├── 用戶管理
└── 組織管理

優點：層級清晰，功能歸屬明確
```

---

## 架構對比圖

### 當前架構 (Current)
```
┌─────────────────────────────────────────────┐
│          Application Routes                  │
│                                              │
│  ┌──────────────┐  ┌────────────────────┐  │
│  │  Blueprints  │  │   Module Manager   │  │
│  │              │  │  (Blueprint-scoped)│  │  ← ❌ 分離
│  │ - List       │  │  ❌ 位置錯誤       │  │
│  │ - Detail     │  │                    │  │
│  │   - Overview │  └────────────────────┘  │
│  │   - Members  │                           │
│  │   - Modules/ │  ┌────────────────────┐  │
│  │     - Issues │  │    Monitoring      │  │
│  │     - Cloud  │  │  (Global)          │  │
│  └──────────────┘  │  ⚠️  可優化        │  │
│                    └────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 重構後架構 (After)
```
┌─────────────────────────────────────────────┐
│          Application Routes                  │
│                                              │
│  ┌──────────────────────────────┐           │
│  │         Blueprints           │           │
│  │                              │           │
│  │ - List                       │           │
│  │ - Detail (:id)               │           │
│  │   ├── Overview               │           │
│  │   ├── Modules (管理)  ✅     │  ← ✅ 整合
│  │   ├── Members                │           │
│  │   └── Modules/               │           │
│  │       ├── Manager/    ✅     │           │
│  │       ├── Issues/            │           │
│  │       └── Cloud/             │           │
│  └──────────────────────────────┘           │
│                                              │
│  ┌──────────────────────────────┐           │
│  │    Admin (System)      ✅    │           │
│  │                              │           │
│  │ - Monitoring                 │  ← ✅ 重命名
│  │ - Users     (未來)           │           │
│  │ - Settings  (未來)           │           │
│  └──────────────────────────────┘           │
└─────────────────────────────────────────────┘
```

---

## 實施影響分析

### 變更影響範圍

#### Module-Manager 重構
```
受影響檔案:
├── src/app/routes/
│   ├── routes.ts                        (修改：移除舊路由，新增重定向)
│   ├── module-manager/                  (移動：整個目錄)
│   └── blueprint/
│       ├── routes.ts                    (修改：新增子路由)
│       ├── blueprint-detail.component.ts (修改：新增 Tab/子路由)
│       └── modules/
│           └── manager/                 (新增：移動後的目錄)

測試影響:
├── E2E 測試                             (更新：路由變更)
├── 單元測試                             (更新：元件變更)
└── 整合測試                             (更新：路由整合)

文檔影響:
├── README.md                            (更新：路由說明)
├── AGENTS.md                            (更新：架構說明)
└── API 文檔                             (更新：URL 範例)
```

#### Monitoring 重構
```
受影響檔案:
├── src/app/routes/
│   ├── routes.ts                        (修改：重命名路由，新增重定向)
│   └── monitoring/                      (保持：檔案位置不變)

新增檔案:
└── src/app/core/guards/
    └── system-admin.guard.ts            (新增：權限檢查)

測試影響:
├── E2E 測試                             (更新：路由變更)
└── 權限測試                             (新增：Guard 測試)

文檔影響:
└── README.md                            (更新：路由說明)
```

---

## 總結與建議

### 關鍵決策點

#### 1. Module-Manager 必須重構 🔴
- **原因**: 嚴重違反架構設計，影響可維護性和用戶體驗
- **優先級**: 高
- **建議**: 立即執行

#### 2. Monitoring 可選優化 ⚠️
- **原因**: 當前位置合理，但缺乏擴展性
- **優先級**: 中
- **建議**: 視資源情況決定

### 實施建議

```
階段規劃:
├── Phase 1: Module-Manager 重構        (必須，7.5 小時)
│   ├── 1.1 備份和移動檔案            (1 小時)
│   ├── 1.2 更新路由配置              (2 小時)
│   ├── 1.3 更新元件和整合            (2 小時)
│   ├── 1.4 測試和驗證                (2 小時)
│   └── 1.5 文檔更新                  (0.5 小時)
│
└── Phase 2: Monitoring 優化           (可選，3.5 小時)
    ├── 2.1 重命名路由                (0.5 小時)
    ├── 2.2 新增權限檢查              (1 小時)
    ├── 2.3 更新導航和文檔            (1 小時)
    └── 2.4 測試和驗證                (1 小時)
```

---

**文檔版本**: 1.0  
**撰寫日期**: 2025-12-21  
**狀態**: 視覺化參考  
**作者**: GigHub 開發團隊
